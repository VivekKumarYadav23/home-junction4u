HOME JUNCTION4U — FLAT ROOT DEPLOYMENT

Upload ALL files from this folder directly into the existing GitHub repository
root, replacing the matching filenames. No asset folders are needed. Keep
index.html, style.css, main.js, favicon.png and every .webp file at the SAME level.
Include the supplied .nojekyll file (it may be hidden in a file manager).

GitHub Pages: Settings > Pages > Deploy from a branch > your publishing branch
> / (root). No Node.js, framework, dependencies or build command is needed.
After GitHub finishes deployment, refresh the browser to clear cached HTML.

The supplied design, CSS, JavaScript, images, content, social links and WhatsApp
message are preserved. Only local asset references were rewritten for flat-root
deployment. HTML page links remain relative for GitHub project subpaths.
The custom 404 embeds its assets and resolves navigation from nested missing URLs.

See VERIFICATION.txt for the checks performed and external-link limitations.
